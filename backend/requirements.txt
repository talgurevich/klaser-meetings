# Web framework
fastapi==0.115.5
uvicorn[standard]==0.32.1

# Database
sqlalchemy==2.0.36
psycopg[binary]==3.2.3
alembic==1.14.0

# Settings
pydantic==2.10.3
pydantic-settings==2.6.1

# HTTP — used by app/services/identity.py to call the identity service
httpx==0.28.1

# Mail — invitation emails (app/services/mail.py). Same version identity uses.
resend==2.4.0

# Utilities
python-dotenv==1.0.1
structlog==24.4.0

# Dev
pytest==8.3.4
pytest-asyncio==0.24.0
ruff==0.8.4
